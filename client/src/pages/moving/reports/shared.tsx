// Re-exports the storage side's generic report helpers so moving reports
// match the same look (chart styling, CSV export, stat cards) without duplication.
export { CHART_STYLE, PIE_COLORS, downloadCsv, StatCard } from '../../reports/shared'
